import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  fieldsCont: {
    backgroundColor: '#F5F5F5',
    marginHorizontal: 20,
    borderRadius: 10,
    padding: 15,
    marginTop: 10,
  },
});
